[project]
name = "slapstick-enhancer-mcp"
version = "2.0.0"
description = "MCP server for enhancing image prompts with slapstick design principles using categorical olog architecture"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
